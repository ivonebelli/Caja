# ==========================================================
# Archivo de Ejemplo de Variables de Entorno (.env.example)
# NOTA: Renombrar este archivo a .env y completar los valores
# ----------------------------------------------------------

# --- 🌐 CONFIGURACIÓN DE BASE DE DATOS REMOTA (Main Process) ---
# Usadas en dbService.js para la conexión remota y segura.
# Estas variables son cruciales para el Pool de Conexiones.

DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_DATABASE=

# --- ⚙️ CONFIGURACIÓN ADICIONAL DEL PROYECTO (Opcional) ---

# URL base para cualquier API externa o microservicio si fuera necesario.
API_BASE_URL=

# Token de autenticación de algún servicio externo (ej. Stripe, Mapas)
EXTERNAL_API_TOKEN=

# Si tienes un ambiente de desarrollo o producción (Electron ya usa NODE_ENV)
# ENV_APP=development